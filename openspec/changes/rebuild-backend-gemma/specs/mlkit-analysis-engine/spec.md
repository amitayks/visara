# mlkit-analysis-engine — Delta Spec (capability retired)

## REMOVED Requirements

### Requirement: MlKitEngine is the default Tier-0 engine
**Reason**: The Tier-0 'mlkit' engine (EfficientNet classification + CRAFT/CRNN OCR on executorch) is deleted; Gemma 4 produces caption, tags, and in-photo text in one pass.
**Migration**: Superseded by `gemma-vision-enrichment`.

### Requirement: MlKitEngine preserves the failure fallback
**Reason**: The Tier-0 'mlkit' engine (EfficientNet classification + CRAFT/CRNN OCR on executorch) is deleted; Gemma 4 produces caption, tags, and in-photo text in one pass.
**Migration**: Superseded by `gemma-vision-enrichment`.
