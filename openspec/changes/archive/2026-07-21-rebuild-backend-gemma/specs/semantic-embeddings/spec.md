# semantic-embeddings — Delta Spec (capability retired)

## REMOVED Requirements

### Requirement: Embeddings table stores per-media vectors
**Reason**: MiniLM embeddings violated the Gemma-only mandate and are deleted with their WatermelonDB storage.
**Migration**: Superseded by `gemma-embedding-index` (EmbeddingGemma-300M, vec0 storage).

### Requirement: Vector payload is serialized for WatermelonDB storage
**Reason**: MiniLM embeddings violated the Gemma-only mandate and are deleted with their WatermelonDB storage.
**Migration**: Superseded by `gemma-embedding-index` (EmbeddingGemma-300M, vec0 storage).

### Requirement: Embeddings are model-versioned for re-embedding
**Reason**: MiniLM embeddings violated the Gemma-only mandate and are deleted with their WatermelonDB storage.
**Migration**: Superseded by `gemma-embedding-index` (EmbeddingGemma-300M, vec0 storage).
