/**
 * @features/onboarding — first-launch step flow (onboarding-experience +
 * onboarding-model-step specs). The nav-app agent registers OnboardingScreen
 * on the `Onboarding` route, gated by settingsStore.onboardingCompleted.
 */
export { OnboardingScreen } from "./OnboardingScreen";
