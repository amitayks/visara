/**
 * @app/shell — ShellScreen and its parts (app-navigation-shell spec).
 * The navigator registers ShellScreen; the rest is exported for tests.
 */
export { BottomBar } from "./BottomBar";
export { PagerShell, type PagerShellProps } from "./PagerShell";
export { ShellScreen } from "./ShellScreen";
export { useShellBackHandler } from "./useShellBackHandler";
